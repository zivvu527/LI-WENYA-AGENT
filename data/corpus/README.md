# Corpus

The public runtime uses prepared voice evidence from:

```text
data/index/voice_chunks.jsonl
```

`seed_chunks.md` is included as a small readable evidence set.

`raw_transcripts/` contains research material from original video subtitle/transcript work. It is useful for rebuilding indexes, but it is not required for ordinary runtime use and is not included by default in the public release package.
